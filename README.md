# Portfolio Starter

Minimal Vite + React + TypeScript + Tailwind starter scaffold with sample content.

Run locally:

```bash
npm install
npm run dev
```

Content lives under `src/content/` as JSON and can be edited directly or replaced with a CMS.
